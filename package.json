{
  "name": "gm-dapp-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild gm.js --bundle --minify --sourcemap --outfile=dist/gm.bundle.js",
    "build:watch": "esbuild gm.js --bundle --sourcemap --outfile=dist/gm.bundle.js --watch",
    "build:old": "esbuild gm_test2.js --bundle --minify --sourcemap --outfile=dist/gm_test2.bundle.js"
  },
  "dependencies": {
    "@reown/appkit": "^1.8.10",
    "@reown/appkit-adapter-ethers": "^1.8.10",
    "ethers": "^6.15.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
